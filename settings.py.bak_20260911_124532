# settings.py

import os
from pathlib import Path

# Define the base directory of the project
BASE_DIR = Path(__file__).resolve().parent

# Define the data directory for storing uploaded files
DATA_DIR = BASE_DIR / "data"
DATA_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated summaries
SUMMARY_DIR = BASE_DIR / "summaries"
SUMMARY_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated key points
KEY_POINTS_DIR = BASE_DIR / "key_points"
KEY_POINTS_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated action items
ACTION_ITEMS_DIR = BASE_DIR / "action_items"
ACTION_ITEMS_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated risks
RISKS_DIR = BASE_DIR / "risks"
RISKS_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated missing information
MISSING_INFO_DIR = BASE_DIR / "missing_info"
MISSING_INFO_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated contradictions
CONTRADICTIONS_DIR = BASE_DIR / "contradictions"
CONTRADICTIONS_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated unusual data
UNUSUAL_DATA_DIR = BASE_DIR / "unusual_data"
UNUSUAL_DATA_DIR.mkdir(exist_ok=True)

# Define the directory for storing AI-generated summaries in different lengths
SUMMARY_LENGTHS = ["short", "medium", "detailed"]

# Define the supported languages
SUPPORTED_LANGUAGES = ["ar", "en"]